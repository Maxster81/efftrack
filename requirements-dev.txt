# Dipendenze di sviluppo (non necessarie in produzione)
# Installazione: .venv/bin/pip install -r requirements-dev.txt
-r requirements.txt
pytest>=8.0.0
# TestClient (Starlette/FastAPI) usa `httpx` per le integrazioni HTTP.
httpx>=0.27.0
